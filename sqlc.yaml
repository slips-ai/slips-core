version: "2"
sql:
  - schema: "migrations"
    queries: "features/task/infra/postgres/queries"
    engine: "postgresql"
    gen:
      go:
        package: "postgres"
        out: "features/task/infra/postgres"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
  - schema: "migrations"
    queries: "features/tag/infra/postgres/queries"
    engine: "postgresql"
    gen:
      go:
        package: "postgres"
        out: "features/tag/infra/postgres"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
